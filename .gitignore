bin/
results/

# bpf2go generated files — regenerated by 'make generate'
# vmlinux.h is intentionally NOT ignored — generate it once on Linux with
# 'make vmlinux' then commit it so macOS cross-compilation works offline.
ebpf-agent/internal/loader/*_bpfel.go
ebpf-agent/internal/loader/*_bpfeb.go
ebpf-agent/bpf/*.o
